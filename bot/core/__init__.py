"""Hexagonal architecture core — domain models, ports, and use cases.

This package contains framework-independent business logic:
- ``models`` — value objects (ChatSession, StreamState, UserMessage)
- ``ports`` — protocols (AICompletionPort, SessionRepository, StreamTracker)
- ``use_cases`` — business logic orchestration
"""
